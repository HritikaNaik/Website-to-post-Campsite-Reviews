<% layout('layout/boilerplate') %>

        <h1>
                <%= campground.title %>
        </h1>
        <h3>
                <%= campground.location %>
        </h3>
        <img src="<%= campground.image  %> " alt="">
        <p>
                <%= campground.description %>
        </p>
        <h2>Price:<i>&dollar;<%= campground.price %> </i></h2>
        <a href="/campgrounds/<%= campground._id%>/edit "><button>Edit</button></a>
        <form action="/campgrounds/<%= campground._id%>?_method=DELETE" method="POST"><button>Delete</button>
        </form>
        <a href="/campgrounds"><button>Back</button></a>